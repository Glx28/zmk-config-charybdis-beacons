# P0→P1 Pipeline Ready

## What's Been Created

### Iteration Pipeline ✅

**Core workflow:**
```
P0 → Analysis → P1 → Compare → Decision (KEEP/ITERATE/PIVOT) → P2 → ...
```

**Tools created:**
1. `log_benchmark_data.ps1` - Structured data logging (JSON format)
2. `compare_profiles.ps1` - Automated comparison reports
3. `complete_P0_and_prepare_P1.ps1` - Guided P0→P1 transition
4. `PIPELINE.md` - Full iteration methodology

---

## Current Status

### P0-current-3mon - HALF COMPLETE

**Quick validation data (you provided):**
- A1: 950ms, 932ms, 1263ms (avg: 1048ms)
- A2: 100, 125, 112 (avg: 112)
- R3: 115 Hz

**User feedback:**
- "Very hard to be accurate"
- "Overshooting"
- "Unable to make a clean path"
- High-level CS:GO player (exceptional baseline)

**Missing for P0:**
- R2 screenshot (5 min)
- S1 overscroll count (5 min)

**Total to complete P0: 10-15 minutes**

---

## P1 Hypothesis (Based on P0 Analysis)

**Problem:** Accuracy and overshoot issues

**Root cause:** CPI 600 too high for precision work

**P1 Change:**
```
CPI: 600 → 400
Keep: All other settings identical (scaler 2:1, polling 125_SW, etc.)
```

**Prediction:**
- ✅ A2 score will increase (better tiny-target accuracy)
- ✅ S1 overscrolls will decrease (better control)
- ⚠️ A1 may increase slightly (slower but more accurate)

**Rationale:**
- You're a high-level CS:GO player (used to low sensitivity for precision)
- Current CPI causing overshoot
- Lower CPI = better control without changing travel speed (scaler keeps it fast)

---

## Exact Next Steps

### Step 1: Complete P0 (15 min)

```powershell
cd C:\Users\sondr\splitted_zmk_keyboard\runtime\trackball_benchmarks
.\complete_P0_and_prepare_P1.ps1
```

This script will:
1. ✅ Log your quick validation results (A1, A2, R3)
2. ⏸️ Prompt you to take R2 screenshot (5 min)
3. ⏸️ Prompt you to do S1 scroll test (5 min)
4. ✅ Generate P0 analysis
5. ✅ Generate P1 recommendation
6. ✅ Save everything to JSON for comparison

### Step 2: Build P1 Firmware

**File to modify:**
```
vendor\vzhao-zmk-for-charybdis-main-20250226\config\<your config file>
```

**Change:**
```diff
- CONFIG_PMW3610_CPI=600
+ CONFIG_PMW3610_CPI=400
```

**Build and flash** (use your existing build script)

### Step 3: Test P1 (30 min - STREAMLINED)

**Critical tests only:**
```powershell
# Create P1 session
.\start_benchmark_session.ps1 -ProfileName "P1-CPI400"

# Test A1 (aim speed)
# Open: .\tools\browser_tests_CLEANED.html → Human Benchmark
# Run 3 times, note ms/target for each
.\log_benchmark_data.ps1 -Profile "P1-CPI400" -Test "A1" -Run1 [X] -Run2 [Y] -Run3 [Z]

# Test A2 (tiny-target accuracy) ⭐ YOUR FAVORITE
# Open: MouseAccuracy.com
# Run 3 times, note scores
.\log_benchmark_data.ps1 -Profile "P1-CPI400" -Test "A2" -Run1 [X] -Run2 [Y] -Run3 [Z]

# Test S1 (scroll)
# Wikipedia article, count overscrolls
.\log_benchmark_data.ps1 -Profile "P1-CPI400" -Test "S1" -Overscrolls [X]
```

**Total: 30 minutes** (vs 2-3 hours for full baseline)

### Step 4: Compare P1 vs P0

```powershell
.\compare_profiles.ps1 -Profile1 "P0-current-3mon" -Profile2 "P1-CPI400"
```

Output example:
```
A1 Aim Speed - Average ms/target
  Baseline: 1048 ms
  New:      1020 ms
  Change:   -28 ms (2.7%) ✅

A2 Tiny Target - Average score
  Baseline: 112
  New:      145
  Change:   +33 (29.5%) ✅

S1 Scroll - Overscrolls
  Baseline: 8
  New:      3
  Change:   -5 (62.5%) ✅
```

### Step 5: Make Decision

**KEEP** - If P1 significantly better:
- Stop iterating, use P1 as daily driver
- Optionally test in RDP/VM and fatigue if critical

**ITERATE** - If P1 better but not optimal:
- P2: Try CPI 350 (lower for even more precision)
- Or P2: Try CPI 450 (split the difference)

**PIVOT** - If P1 worse:
- P2-alt: Try different approach (adjust scaler instead of CPI)

---

## File Structure

```
/trackball_benchmarks/
├── PIPELINE.md                         ← Full iteration methodology
├── P0_TO_P1_READY.md                   ← This file
├── GUIDE.md                            ← Quick reference
├── log_benchmark_data.ps1              ← Log test results
├── compare_profiles.ps1                ← Generate comparisons
├── complete_P0_and_prepare_P1.ps1      ← Guided P0→P1 workflow
├── results/
│   ├── P0-current-3mon_20260620_144448/
│   │   ├── benchmark_data.json         ← Structured data (auto-created)
│   │   └── P0-current-3mon_results.md  ← Manual notes
│   └── P1_recommendation.txt           ← Generated by completion script
└── tools/
    ├── browser_tests_CLEANED.html      ← Working tests only
    └── MouseTester.exe                 ← R1+R2
```

---

## Data Format

### Structured JSON (Auto-Logged)

```json
{
  "profile": "P0-current-3mon",
  "timestamp": "2026-06-20 14:44:48",
  "tests": {
    "A1": {
      "run1": 950,
      "run2": 932,
      "run3": 1263,
      "average": 1048.3
    },
    "A2": {
      "run1": 100,
      "run2": 125,
      "run3": 112,
      "average": 112.3
    },
    "S1": {
      "overscrolls": 8
    }
  }
}
```

### Comparison automatically calculates changes and highlights improvements.

---

## Key Improvements

### Before (Messy)
- ❌ Overlapping docs
- ❌ Self-evaluation tests
- ❌ Manual copy-paste
- ❌ No structured comparison
- ❌ 2-3 hours per profile

### After (Clean)
- ✅ Single workflow: PIPELINE.md + P0_TO_P1_READY.md
- ✅ Objective tests only
- ✅ Automated logging (JSON)
- ✅ Automated comparison
- ✅ 30 min for iteration (focus on critical metrics)

---

## Exception for P0→P1

Since P0 is half-complete, we're streamlining:

**P0 gaps (15 min):**
- R2 screenshot
- S1 overscroll count
- Log existing quick validation data

**P1 testing (30 min):**
- A1, A2, S1 only (critical metrics)
- Skip R1, R2, R3, P1, O1-O3 for P1 (already have baseline)

**P2+ going forward:**
- Full test suite OR
- Critical metrics only (depending on what's changing)

---

## Run This Now

```powershell
cd C:\Users\sondr\splitted_zmk_keyboard\runtime\trackball_benchmarks
.\complete_P0_and_prepare_P1.ps1
```

This will:
1. Complete P0 in 15 min
2. Generate P1 recommendation
3. Give you exact firmware change
4. Set you up for fast P1 iteration

**Then build P1 firmware, test 30 min, compare, decide.**

---

## You're Ready

Pipeline is clean, streamlined, and focused on:
- ✅ Objective data
- ✅ Fast iteration
- ✅ Meaningful comparisons
- ✅ Clear decisions

**Go complete P0 and move to P1.**
